% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fitness.R
\name{plot_mix_fitness}
\alias{plot_mix_fitness}
\title{Draw plots to compare fitness measures}
\usage{
plot_mix_fitness(
  data,
  var_names = NULL,
  mix_scale = c("fraction", "ratio"),
  color = c(NULL, NULL, NULL),
  fill = c(NULL, NULL, NULL),
  shape = NULL,
  size = NULL,
  drop_NA = TRUE
)
}
\arguments{
\item{data}{Data frame of fitness values. Wide format: each row must contain
data for two microbes in the same population. Accepts data frame
extensions like \code{tibble}.}

\item{var_names}{Named character vector identifying fitness and mixing
variables in \code{data}. If \code{NULL}, defaults to column names returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}. See Details.}

\item{mix_scale}{Mixing frequency scale for x axis. \code{"fraction"} uses
initial frequency of strain A (proportion of total) from
\code{initial_fraction_A} variable in data. \code{"ratio"} uses ratio of strain A to
strain B (on \eqn{\log_{10}} scale) from \code{initial_ratio_A_B}.
Defaults to show both.}

\item{color}{Point colors \code{c(strain_A, strain_B, total_group)}}

\item{fill}{Point fill colors \code{c(strain_A, strain_B, total_group)}.
Only affects shapes 21--25.}

\item{shape}{Point shape}

\item{size}{Point size in mm}

\item{drop_NA}{\code{TRUE} to silently remove missing values.
\code{FALSE} to warn when removing missing values.}
}
\value{
A ggplot object
}
\description{
\code{plot_mix_fitness()} shows a diagnostic overview of various fitness effects
in a dataset. Draws a combined plot of strain, total group, and relative
within-group fitness against two measures of mix frequency.
}
\details{
Expects Wrightian fitness measures like those returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}.

\code{var_names} must be a named vector or list that includes the following
elements (shown here with default values):

\if{html}{\out{<div class="sourceCode">}}\preformatted{var_names = c(
  name_A = "name_A",
  name_B = "name_B",
  initial_fraction_A = "initial_fraction_A",
  initial_ratio_A_B = "initial_ratio_A_B",
  fitness_A = "fitness_A",
  fitness_B = "fitness_B",
  fitness_total = "fitness_total",
  fitness_ratio_A_B = "fitness_ratio_A_B"
)
}\if{html}{\out{</div>}}
}
\examples{
fitness_myxo <- calculate_mix_fitness(data_smith_2010, var_names_smith_2010)
plot_mix_fitness(fitness_myxo)

# Some plot options
plot_mix_fitness(
  fitness_myxo,
  mix_scale = "fraction",
  color = c("black", "grey40", "grey40"),
  fill = c("grey50", "white", "grey75"),
  shape = 23,
  size = 2
)

}
\seealso{
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}
}
