% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axes.R
\name{scale_y_fitness_total}
\alias{scale_y_fitness_total}
\title{Y-axis for fitness of groups or subpopulations}
\usage{
scale_y_fitness_total(
  name = waiver(),
  limits = NULL,
  breaks = waiver(),
  labels = waiver(),
  minor_breaks = waiver(),
  ...
)
}
\arguments{
\item{name}{Character vector (or expression) for axis title. Or \code{waiver()}
to automatically name axis.}

\item{limits}{Numeric vector of length two giving axis limits. Or \code{NULL} for
automatic limits that include 1 for reference (no change in abundance)
and span a minimum 10-fold range.}

\item{breaks}{Numeric vector of positions for axis breaks.
Or \code{waiver()} for automatic breaks.
Or \code{NULL} for no breaks.}

\item{labels}{One of:
\itemize{
\item Character vector giving labels for breaks (must be same length as
\code{breaks})
\item \code{waiver()} for automatic labels that use a simple number format when all
breaks are between 0.01 and 100. For wider limits they use \eqn{10^x}
format except for \eqn{1}.
\item Expression vector (must be the same length as \code{breaks}). See ?plotmath
for details.
\item \code{NULL} for no labels
}}

\item{minor_breaks}{Numeric vector of positions for axis minor breaks.
Or \code{waiver()} for automatic minor breaks.
Or \code{NULL} for no breaks.}

\item{...}{Other arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_log10()}}}
}
\value{
Y-axis position scale for use with the ggplot2 package
}
\description{
\code{scale_y_fitness_total()} is a y-axis scale for the total fitness of
microbial groups or subpopulations. It calls \code{ggplot2::scale_y_log10()} with
default settings suited to Wrightian fitness data.
}
\details{
\code{scale_y_fitness_total()} expects data that are absolute (unscaled) Wrightian
fitness, like the values produced by \code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}.
If the initial abundance of a microbial group is \eqn{n} (measured as cfu/mL
for example) and its final abundance is \eqn{n'}, then the group's Wrightian
fitness measured over that entire time period is \eqn{w = n' / n}.
}
\examples{
library("ggplot2")
fitness_myxo <- calculate_mix_fitness(data_smith_2010, var_names_smith_2010)
fig <- fitness_myxo |>
  ggplot(aes(initial_fraction_A, fitness_total)) +
  geom_point_overlap()

fig + scale_y_log10()
fig + scale_y_fitness_total()

# Axis options
fig + scale_y_fitness_total(
  name = "Sporulation efficiency\n(spores/cell)",
  limits = c(1e-8, 1)
)

}
\seealso{
\code{\link[=scale_y_fitness]{scale_y_fitness()}}, \code{\link[=scale_y_fitness_ratio]{scale_y_fitness_ratio()}}
}
