% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom.R
\name{geom_point_overlap}
\alias{geom_point_overlap}
\title{Points with better overlap readability}
\usage{
geom_point_overlap(...)
}
\arguments{
\item{...}{Arguments passed to \code{geom_point()}.}
}
\description{
This is a small modification to \code{geom_point()} from ggplot2 that makes data
points easier to see when they overlap. \code{geom_point_overlap()} first draws
points as normal then draws points again with no fill. It only has a visible
effect effect on points with separate color and fill (shapes 21-25).
}
\examples{
library(ggplot2)
fig <- ggplot(data = airquality) + aes(x = Temp, y = Ozone)
fig + geom_point(shape = 21, size = 2, fill = "grey", na.rm = TRUE)
fig + geom_point_overlap(shape = 21, size = 2, fill = "grey", na.rm = TRUE)

}
