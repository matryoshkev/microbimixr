% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom.R
\name{geom_point_overlap}
\alias{geom_point_overlap}
\title{Points with better overlap readability}
\usage{
geom_point_overlap(...)
}
\arguments{
\item{...}{Arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\description{
\code{geom_point_overlap()} is a version of \code{geom_point()} from gggplot2 that is
easier to see when points overlap.
It first draws points as normal then draws points again with no fill.
It only has a visible effect on point shapes 21-25.
}
\examples{
library(ggplot2)
fig <- ggplot(data = airquality, mapping = aes(x = Temp, y = Ozone))
fig + geom_point(shape = 21, size = 3, fill = "grey65", na.rm = TRUE)
fig + geom_point_overlap(size = 3, na.rm = TRUE)

}
