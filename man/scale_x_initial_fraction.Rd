% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axes.R
\name{scale_x_initial_fraction}
\alias{scale_x_initial_fraction}
\title{X-axis for initial strain frequency}
\usage{
scale_x_initial_fraction(
  name = waiver(),
  limits = NULL,
  breaks = waiver(),
  minor_breaks = NULL,
  ...
)
}
\arguments{
\item{name}{Character string or expression to label axis.
Or \code{waiver()} to automatically name axis.
Or \code{NULL} for no title.}

\item{limits}{Numeric vector of length two giving axis limits.
Or \code{NULL} for automatic limits \code{c(0, 1)} showing the full range of possible
mix frequencies.}

\item{breaks}{Numeric vector of positions for axis breaks.
Or \code{waiver()} for automatic breaks.
Or \code{NULL} for no breaks.}

\item{minor_breaks}{Numeric vector of positions for axis minor breaks.
Or \code{NULL} for no minor breaks.}

\item{...}{Other arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}}}
}
\value{
X-axis position scale for use with the ggplot2 package
}
\description{
\code{scale_x_initial_fraction()} is an x-axis position scale for initial strain
frequency (fraction or proportion of total) measured on a linear scale. It
calls \code{ggplot2::scale_x_continuous()} with default settings suited to
microbial mix experiments.
}
\examples{
library("ggplot2")
fitness_myxo <- calculate_mix_fitness(data_smith_2010, var_names_smith_2010)
fig <- fitness_myxo |>
  ggplot(aes(initial_fraction_A, fitness_total)) +
  geom_point_overlap() +
  scale_y_log10()

fig
fig + scale_x_initial_fraction()

# Manually label axis
fig + scale_x_initial_fraction(name = "Initial frequency of evolved strain")

}
\seealso{
\code{\link[=scale_x_initial_ratio]{scale_x_initial_ratio()}}
}
