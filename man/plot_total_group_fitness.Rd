% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fitness.R
\name{plot_total_group_fitness}
\alias{plot_total_group_fitness}
\title{Plot fitness of total group or subpopulation}
\usage{
plot_total_group_fitness(
  data,
  var_names = NULL,
  mix_scale = "fraction",
  xlab = NA,
  ylab = NA,
  xlim = c(NA, NA),
  ylim = c(NA, NA),
  color = "black",
  fill = "grey65",
  shape = 21,
  size = 1.5
)
}
\arguments{
\item{data}{Data frame of fitness values and mix frequencies.
Accepts data frame extensions like \code{tibble}.}

\item{var_names}{Named character vector identifying fitness and mixing
variables in \code{data}. If \code{NULL}, defaults to column names returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}. See Details.}

\item{mix_scale}{Determines mixing scale for x axis. When \code{"fraction"}
(the default), uses initial frequency of strain A (proportion of total)
from \code{initial_fraction_A} variable. When \code{"ratio"}, uses ratio of strain A
to strain B (on \eqn{\log_{10}} scale) from \code{initial_ratio_A_B} variable.}

\item{xlab, ylab}{Optional string to replace default axis label}

\item{xlim, ylim}{Optional axis limits to replace default}

\item{color}{Point color}

\item{fill}{Point fill. Only affects shapes 21-25.}

\item{shape}{Point shape}

\item{size}{Point size in millimeters}
}
\value{
A ggplot object that can be further modified using the ggplot2 package
}
\description{
\code{plot_total_group_fitness()} draws a plot of total-group fitness as a
function of initial strain frequency
}
\details{
Expects Wrightian fitness data like those returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}.

\code{var_names} must be a named vector or list that includes the following
elements (shown here with default values):

\if{html}{\out{<div class="sourceCode">}}\preformatted{var_names = c(
  name_A = "name_A",
  name_B = "name_B",
  initial_fraction_A = "initial_fraction_A",
  initial_ratio_A_B = "initial_ratio_A_B",
  fitness_total = "fitness_total"
)
}\if{html}{\out{</div>}}
}
\examples{
fitness_myxo <- calculate_mix_fitness(data_smith_2010, var_names_smith_2010)
plot_total_group_fitness(fitness_myxo)

# Using ratio scale for mix frequencies
plot_total_group_fitness(fitness_myxo, mix_scale = "ratio")

# Other plot options
plot_total_group_fitness(
  fitness_myxo,
  ylim = c(1e-8, 1),
  xlab = "Initial frequency of evolved strain",
  ylab = "Sporulation efficiency\n(spores / cell)",
  color = "darkblue",
  fill = "lightblue",
  shape = 23,
  size = 2
)

}
\seealso{
\code{\link[=plot_within_group_fitness]{plot_within_group_fitness()}}, \code{\link[=plot_strain_fitness]{plot_strain_fitness()}}
}
