% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fitness.R
\name{plot_within_group_fitness}
\alias{plot_within_group_fitness}
\title{Plot within-group ratio of strain fitnesses}
\usage{
plot_within_group_fitness(
  data,
  var_names = NULL,
  mix_scale = "fraction",
  xlab = NA,
  ylab = NA,
  xlim = c(NA, NA),
  ylim = c(NA, NA),
  color = NULL,
  fill = NULL,
  shape = NULL,
  size = NULL,
  drop_NA = TRUE
)
}
\arguments{
\item{data}{Data frame of fitness values and mix frequencies.
Accepts data frame extensions like \code{tibble}.}

\item{var_names}{Named character vector identifying fitness and mixing
variables in \code{data}. If \code{NULL}, defaults to column names returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}. See Details.}

\item{mix_scale}{Mixing scale for x axis. Default \code{"fraction"} uses initial
frequency of strain A (proportion of total) from \code{initial_fraction_A}
variable of data. \code{"ratio"} uses ratio of strain A to strain B (on
\eqn{\log_{10}} scale) from \code{initial_ratio_A_B}.}

\item{xlab, ylab}{X and y axis labels}

\item{xlim, ylim}{X and y axis limits}

\item{color}{Point color}

\item{fill}{Point fill color. Only affects shapes 21-25.}

\item{shape}{Point shape}

\item{size}{Point size in mm}

\item{drop_NA}{\code{TRUE} to silently remove missing values.
\code{FALSE} to warn when removing missing values.}
}
\value{
A ggplot object that can be further modified using the ggplot2 package
}
\description{
\code{plot_within_group_fitness()} draws a plot of the relative within-group
fitness of strains A and B as a function of their initial frequency
}
\details{
\code{var_names} must be a named vector or list that includes the following
elements (shown here with default values):

\if{html}{\out{<div class="sourceCode">}}\preformatted{var_names = c(
  name_A = "name_A",
  name_B = "name_B",
  initial_fraction_A = "initial_fraction_A",
  initial_ratio_A_B = "initial_ratio_A_B",
  fitness_ratio_A_B = "fitness_ratio_A_B"
)
}\if{html}{\out{</div>}}

Expects Wrightian fitness data like those returned by
\code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}. Relative within-group fitness is measured as
the ratio of strain A fitness to strain B fitness.
Does not try to plot single-strain data.
}
\examples{
fitness_myxo <- calculate_mix_fitness(data_smith_2010, var_names_smith_2010)
plot_within_group_fitness(fitness_myxo)

# Using ratio scale for mix frequencies
plot_within_group_fitness(fitness_myxo, mix_scale = "ratio")

# More plot options
plot_within_group_fitness(
  fitness_myxo,
  mix_scale = "ratio",
  ylim = c(0.01, 100),
  xlab = "Initial ratio evolved / ancestral",
  ylab = "Relative sporulation success\nevolved / ancestral",
  color = "darkblue",
  fill = "lightblue",
  shape = 23,
  size = 2
)

}
\seealso{
\code{\link[=plot_total_group_fitness]{plot_total_group_fitness()}}, \code{\link[=plot_strain_fitness]{plot_strain_fitness()}}
}
