% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axes.R
\name{scale_y_fitness}
\alias{scale_y_fitness}
\title{Position scale for microbial fitness}
\usage{
scale_y_fitness(
  name = NA,
  limits = NULL,
  breaks = NA,
  labels = NA,
  minor_breaks = NA,
  ...
)
}
\arguments{
\item{name}{Character vector (or expression) for axis title. Or \code{NA}
to automatically name axis.}

\item{limits}{Numeric vector of length two giving axis limits. Or \code{NULL} for
automatic limits that include 1 for reference (no change in abundance)
and span a minimum 10-fold range.}

\item{breaks}{Numeric vector of positions for axis breaks. Or \code{NA} for
automatic breaks. Or \code{NULL} for no breaks.}

\item{labels}{One of:
\itemize{
\item Character vector giving labels for breaks (must be same length as
\code{breaks})
\item \code{NA} for automatic labels that use a simple number format when all breaks
are between 0.01 and 100. For wider limits they use \eqn{10^x} format
except for \eqn{1}.
\item Expression vector (must be the same length as \code{breaks}). See ?plotmath
for details.
\item \code{NULL} for no labels
}}

\item{minor_breaks}{Numeric vector of positions for axis minor breaks. Or
\code{NA} for automatic breaks. Or \code{NULL} for no breaks.}

\item{...}{Other arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_log10()}}.}
}
\description{
\code{scale_y_fitness()} is a y-axis scale for the fitness of microbial strains
and groups. It calls \code{ggplot2::scale_y_log10()} with default settings suited
to Wrightian fitness data.
}
\details{
\code{scale_y_fitness()} expects data that are absolute (unscaled) Wrightian
fitness, like the values produced by \code{\link[=calculate_mix_fitness]{calculate_mix_fitness()}}.
If the initial abundance of some microbe is \eqn{n} (measured as cfu/mL
for example) and its final abundance is \eqn{n'}, then its Wrightian
fitness measured over that entire time period is \eqn{w = n' / n}.
}
\seealso{
\code{\link[=scale_y_fitness_total]{scale_y_fitness_total()}}, \code{\link[=scale_y_fitness_ratio]{scale_y_fitness_ratio()}}
}
